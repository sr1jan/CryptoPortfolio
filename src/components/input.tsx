import React, {Component} from 'react';
import {
  StyleSheet,
  ScrollView,
  View,
  Text,
  Alert,
  StatusBar,
  TouchableOpacity,
  TextInput,
} from 'react-native';

import {Input, Button, Icon} from 'react-native-elements';
